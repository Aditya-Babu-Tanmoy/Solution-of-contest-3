/// Sort a string after one element
/*#include<iostream>
#include<algorithm>
using namespace std;

int main()
{
    string s;
    cin>>s;
    sort(s.begin(), s.end());
    for(int i=s.length()/2; i<s.length(); i++)// use length/2 to omitted the '+' symbol
    {
        cout<<s[i];
        if(i<s.length()-1)
        cout<<"+";
    }
    return 0;
}*/
